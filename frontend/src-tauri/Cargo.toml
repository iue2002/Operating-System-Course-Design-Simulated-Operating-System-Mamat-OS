[package]
name = "os-sim-frontend"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "1.5", features = [ "dialog-all"] }
reqwest = { version = "0.11", features = ["blocking"] }

[build-dependencies]
tauri-build = { version = "1.5", features = [] }
