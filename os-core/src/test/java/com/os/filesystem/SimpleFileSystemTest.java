package com.os.filesystem;

/**
 * Placeholder test file (no JUnit imports) to avoid build failures in environments
 * where JUnit/jupiter is not available on the classpath. Replace with real tests
 * or restore the original when your environment has Maven/IDE configured to run tests.
 */
public class SimpleFileSystemTest {
    public static void placeholder() {
        // no-op
    }
}
